{
  "private": true,
  "scripts": {
    "start": "node index.js",
    "deploy": "now rm simpleicons --safe -y || true && now"
  },
  "dependencies": {
    "@primer/octicons": "^19.18.0",
    "micro": "^10.0.1",
    "micro-fork": "^3.0",
    "my-way": "^2.0.0",
    "serve-marked": "^5.0.0"
  },
  "engines": {
    "node": "22.x"
  }
}
